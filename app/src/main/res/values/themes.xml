<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="Theme.AndroidIME" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color. -->
        <item name="android:background">@color/gray_200</item>
        <item name="android:colorBackground">@color/gray_200</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/black</item>
        <item name="colorButtonNormal">@color/white</item>
        <item name="colorControlNormal">@color/green_700</item>
        <item name="colorControlHighlight">@color/green_400</item>
        <item name="colorControlActivated">@color/green_200</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?colorControlNormal</item>
        <!-- Nav bar color. -->
        <item name="android:navigationBarColor">?colorControlNormal</item>
    </style>

</resources>
